const TIME_PERIODS=['de prehistorie','Mesopotamië','het oude Egypte'];
const TIME_PICTURES=[{image:'rondtrekken',caption:'Mensen trekken rond op zoek naar voedsel.',period:0},{image:'zoekplaat-boerderij',caption:'De eerste boeren wonen bij hun akkers en dieren.',period:0},{image:'mesopotamie-gebouwen',caption:'Een stad met een ziggurat.',period:1},{image:'piramide',caption:'Piramides bij de Nijl.',period:2}];
const TIME_QUESTIONS=[['Wat begon eerder?',['De prehistorie','Het oude Egypte'],0],['Bestonden Mesopotamië en het oude Egypte ook tegelijk?',['Ja, een deel van de tijd','Nee, Egypte kwam pas daarna'],0],['Op welke prent trekken mensen nog rond?',['Prent 1','Prent 2','Prent 3','Prent 4'],0]];
function timeBand(placed=[],interactive=false,paper=false,solution=false){return `<div class="time-band"><div class="time-arrow">eerder <span>later →</span></div>${TIME_PERIODS.map((name,i)=>`<div class="time-lane lane-${i}">${interactive?`<button class="card time-target" data-period="${i}">${name}</button>`:`<b>${name}</b>`}<div class="time-slots">${paper?(solution?TIME_PICTURES.map((p,j)=>p.period===i?j+1:null).filter(Boolean).join(', '):i===0?'___ en ___':'___'):placed.filter(j=>TIME_PICTURES[j].period===i).map(j=>`<img src="assets/context/${TIME_PICTURES[j].image}.png" alt="Prent ${j+1}">`).join('')}</div></div>`).join('')}</div><p class="time-note">Vereenvoudigde tijdsband, niet op schaal. De twee onderste balken lopen deels tegelijk.</p>`}
function timeHomework(solution=false){return '<h3>Welke prent hoort waar?</h3><p>Schrijf de nummers van de prenten in de juiste balk. Twee prenten horen bij de prehistorie.</p><div class="time-paper-pictures">'+TIME_PICTURES.map((p,i)=>`<div><b>${i+1}</b><img src="assets/context/${p.image}.png" alt="${p.caption}"><small>${p.caption}</small></div>`).join('')+'</div>'+timeBand([],false,true,solution)+'<h3>Kijk naar de tijdsband. Kruis aan.</h3>'+TIME_QUESTIONS.slice(0,2).map(q=>`<div class="time-paper-question"><b>${q[0]}</b>${q[1].map((a,i)=>`<p>${solution&&i===q[2]?'☑':'☐'} ${a}</p>`).join('')}</div>`).join('')}
